# Template provenance

The generator owns runnable application templates and their dependency locks. FunnySoft standards owns policy, playbook documents and shared engineering assets. Generated applications require no sibling checkout or generation-time download of standards.

## Release state

The generator bundle is version `0.2.0`, with template revision `0.2.0`. It pins published standards `v0.3.1`, merged commit `13889e25ab3df8a06307156722dc08377183b356`, export digest `9617fb25af0eab734dd99ac5fc4d1b8d186b9b5214b6ae4ec5cce70263d9aa42`. The export was produced from `git archive` of that exact commit, not the standards working directory. Package validation does not itself approve npm publication, paid-license scope or live deployment behavior.

## Release manifest, schema 1

A releasable manifest has `status: "release"`, the exact `generatorVersion` from the package, `templateRevision` equal to that release version or a full 40-character commit, and `standards: { release, commit, assetDigest }`. Version identity avoids inventing a future commit while final assets are still being committed. The coordinator verifies that the standards version tag resolves to the tested commit before exporting it. Hashes detect bundle corruption against the shipped pin, they do not attest Git history or authenticate the npm publisher.

`templates` maps each standards variant to a template-relative `root`. `assets` inventories every file beneath `template/`, except this manifest and the separately verified `standards/` export. Each entry declares `path`, SHA-256 `sha256`, numeric `mode` of 420 or 493, and boolean `text`. Include this provenance notice, license notices, extras, locks, nested dotfiles and executable scripts. Unknown inventory files fail verification.

The standards schema 1 manifest binds its runtime hash, every asset hash, path, mode, text policy, layout and immutable identity through `assetDigest`. The generator checks it independently before importing `apply.mjs`, then calls the shared `verifyExport` and `applyExport`. Standards writes its own receipts. `F7T_MANIFEST.json` records generator, template and standards identity. The bundled runtime requires Node.js 22 or newer.

## Composed dependency locks

`locks` entries contain `key`, `manifests` and `files`. Build the key with `compositionKey` using all normalized dependency-affecting choices, including the stack. `manifests` maps generated package or Composer manifest paths to `dependencyDigest` values. `files` declares template-relative `source`, target-relative `destination` and `sha256` for each tested lock. Sources must also appear in `assets`.

Before dependency installation, call `applyReleaseLock` after composing manifests. Unsupported choices, mismatched dependency declarations and altered locks fail. Install with the package manager's frozen-lock mode and propagate failures. Never repair a frozen-lock error by silently resolving new versions. U14 must enumerate all supported compositions and prove frozen installation for each.

## Copy and distribution policy

Template replacement is limited to the explicit text extensions and filenames in `cli/src/fs.ts`. Unknown types and binary files retain their bytes. Add new text types deliberately. Nested `gitignore` files become `.gitignore`; collisions fail. File executable bits are preserved. Source and destination symlinks fail.

Dependency trees, build output, runtime caches, local databases, environment files other than `.env.example`, authentication files and package archives are rejected. Dependency metadata must contain no authenticated URLs or Composer credentials. Private packages, including Scramble Pro, are installed from authorized configured sources, never redistributed. Lockfiles contain only sanitized repository URLs. Real credentials are not template inputs.

`bun run verify:package` audits the package allowlist and template inventory. `bun run verify:release` additionally requires the complete release manifest. Publishing runs both checks. The fixture package test packs and extracts outside the checkout and exercises the same APIs with a synthetic standards schema 1 export; it does not assign a real standards release.

## U14 candidate tooling

`bun run prepare:candidate --fixture-mode` writes only a new external directory. Its private npm package has an explicit candidate version and a refusing publication hook. Historical audit candidates are not publication evidence. `bun run prepare:release` requires the expected published standards release, commit and digest, checks them against the supplied export, and prepares a normal-version bundle without fixture mode. Neither command publishes or edits the source tree. The coordinator integrates the verified generated standards, locks and manifest, then verifies the actual package.

The catalog enumerates 64 Next selection keys and two fixed Laravel keys through the stack registry. Playwright is already a mandatory dev dependency, so some selection keys share identical dependency graphs; the optional flag adds the smoke-test script and files. Preserve both keys. Shell and locale changes share compatible locks. Composer locks are validated and copied, never regenerated with an update. The packed matrix performs real frozen Bun and locked Composer installs outside the checkout and compares every packed file's bytes to its source, including fonts and notices.

## Third-party rights reviewed for U14

| Material                             | Recorded version/source                                                                            | License and required treatment                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------ | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Laravel starter code                 | Existing template source; dependency framework locks resolve `v13.31.0`                            | MIT. Preserve Taylor Otwell's notice in Inertia `LICENSE` and API `services/api/LICENSE.laravel`.                                                                                                                                                                                                                                                                                                |
| shadcn-derived UI source             | `shadcn-ui/ui`, upstream `LICENSE.md` inspected through GitHub on 2026-09-08                       | MIT, copyright 2023 shadcn. Preserve the added root `NOTICE` in all generated layouts. This license does not establish an exact original component revision, which was not recorded in the inherited template.                                                                                                                                                                                   |
| Space Grotesk font binaries          | `@fontsource-variable/space-grotesk@5.3.0`                                                         | SIL OFL 1.1. Both design homes retain `space-grotesk-OFL.txt` and the source URL.                                                                                                                                                                                                                                                                                                                |
| Public Sans historical mock binaries | `@fontsource-variable/public-sans@5.2.6`                                                           | SIL OFL 1.1. Both design homes retain `OFL.txt` and source provenance.                                                                                                                                                                                                                                                                                                                           |
| Scramble                             | Composer `dedoc/scramble v0.13.42`                                                                 | MIT package metadata. Dependency code is not in the npm inventory.                                                                                                                                                                                                                                                                                                                               |
| Scramble Pro                         | Composer `dedoc/scramble-pro v0.9.15`, source reference `5aa09eacafbfd74d6720c140829d0976af165599` | Lock metadata says `proprietary`. Installed `LICENSE.md` says "The Paid License" and points to `https://scramble.dedoc.co/pro`; it contains no detailed grant. Authorized installation is required, and it does not prove redistribution or downstream project entitlement. The coordinator must confirm the paid license's actual project scope before release. No Pro source is redistributed. |

OFL permits bundling and embedding fonts with software, requires retaining the license/copyright notices, prohibits selling the fonts by themselves, and keeps modified fonts under OFL with its reserved-name restrictions. The bundled bytes are unmodified. Package dependencies such as React, Radix, Inertia and Fontsource are declarations and sanitized lock metadata here; their installed dependency trees are excluded from distribution.

The standards export also needs its redistribution rights cleared at the published revision. A matching SHA-256 proves integrity, not licensing. Historical audit-only identities remain in the verification report, separate from the current published standards pin.

The compiled CLI bundles public dependencies as code. `CLI-NOTICES.txt` preserves the MIT notices for the actual bundled Clack, sisteransi and string-width/wrapping packages. This is distinct from generated application dependency trees, which remain excluded from the package.
